{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle src/client.ts --outfile=../server/public/dist/client.js --minify --sourcemap --target=es6",
    "watch": "esbuild --bundle --watch src/client.ts --outfile=../server/public/dist/client.js --minify --sourcemap --target=es6"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io-client": "^4.1.3",
    "three": "^0.131.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/three": "^0.131.0",
    "@types/uuid": "^8.3.1",
    "esbuild": "^0.12.20"
  }
}
